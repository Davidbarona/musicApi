import React from 'react'
import "./SideBar.css"

import { Button, DatePicker } from 'antd';

const SideBar = () => {
    return (
        <div className="sideBarContainer">
            <Button type="dark" size="large">PROABDNO</Button>
        </div>
    )
}

export default SideBar
